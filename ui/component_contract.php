<?php

/**
 * LEGAiSEE UI COMPONENT CONTRACT
 *
 * RULES:
 * 1. Components must return STRING HTML only
 * 2. No inline styles allowed
 * 3. No database access inside components
 * 4. No business logic inside components
 * 5. Components must use global CSS classes only
 */

interface UIComponent {}