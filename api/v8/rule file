<?php
require_once __DIR__ . '/../../db.php';

header('Content-Type: application/json');

// disable warnings breaking output
error_reporting(0);
ini_set('display_errors', 0);

$response = [];

try {

    // YOUR LOGIC HERE

    $response = ["status" => "ok"];

} catch (Exception $e) {

    $response = ["error" => $e->getMessage()];
}

echo json_encode($response);
exit;