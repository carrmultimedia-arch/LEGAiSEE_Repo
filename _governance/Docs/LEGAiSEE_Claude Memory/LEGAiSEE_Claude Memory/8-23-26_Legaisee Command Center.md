8-23-26_Legaisee Command Center
Summary
LEGAiSEE Command Center — PHP/MySQL app on SureServer; PM module, brain pipeline, ingest linking, live post-sync bug-fixing in progress
Details
PHP/MySQL application hosted on SureServer, database carrmulti_legaiseearchive
Architecture locked: shell.php as sole routing entry, kernel_db() as sole DB access, ui/global.css as sole stylesheet, complete replacement files only (never patches)
Terminology: Grand Lobby (shell.php dashboard, being replaced by Master OS/UI), Wings (module sections), Excavation (ingestion/analysis), Dig Review (human verification interface), Artifact (raw input), Brain (AI reasoning layer)
Nine brain_*.php pipeline files form a coherent excavation-to-dossier pipeline on flat JSON under data/brains/. modules/brain_module.php is only a read-only system health inspector, not the real pipeline — the real excavation pipeline is the WO-D/WO-F module set (entity_resolution_module.php, semantic_cluster_module.php, root_cause_graph_module.php, dig_review_module.php) writing to data/cases/{case_id}/*.json.
Business vision: 5 phases (Authority Dossier -> Full Authority Report -> Living Authority Vault subscription -> cross-client pattern intelligence -> platform licensing). Authority Dossier is Phase 1 paid product. CAMERA Authority Framework™ is the proprietary methodology.
Wants a master umbrella OS/UI unifying all LEGAiSEE modules — cross-talking, self-maintaining, human-language-first, one system not a pile of parts. Wants LEGAiSEE's own luxury brand voice (per legaisee.com) reflected in the OS/UI itself.
Codebase large enough John no longer remembers all parts; includes a ProjectManager sub-app (own MVC) confirmed NOT working yet, and a numbered KnowledgeSystem doc library (000-800).
V1 scope locked: business history restoration + step-by-step Business Authority Marketing System from proven legacy tactics; CAMERA System is the only approved V1 add-on. Priority 1 = UI/human interface, Priority 2 = Project Manager.
V1 build order (WO-A through WO-H) complete: audit, shell nav, ingest, analysis engines (entity_resolution/relations write insights.json, semantic_cluster writes clusters.json, root_cause_graph writes network.json, dig_review reads from those — found+fixed a real "computed but never persisted" gap), view_graph (vis-network, tested 22 nodes/20 edges), review/report(dompdf)/vault, Stripe+email payment backend (Exhibit tier $500-$250k, SMTP confirmed working, still needs a real end-to-end Stripe test-card checkout).
Locked: Clients/Cases uses a HoneyBook-style drag-and-drop Kanban, fully custom stages per client via a stage-builder at intake. Cards open the full Case/Client drill-in (timeline/graph/findings/report/payment/email). Client-facing view mirrors stages read-only.
New scope: real email mailbox integration (IMAP/SMTP via cPanel) into Case/Client drill-in and System Diary — not yet built, WO-V left it as an honest placeholder.
Two distinct dig products: Shallow/Mini Dig ($2k, 1-3 artifacts) vs Full Excavation (40+ channels, full checklist in legaisee-excavation-channels). Confirmed pillar order: Intake -> Excavation -> Dig Review -> Knowledge System -> Case/Client drill-in -> Vault -> Payments -> System Health/PM. Single client record from first touch, "Lead" is the universal default stage.
CRITICAL locked principle: nothing found is ever discarded — raw artifact library is permanent regardless of review outcome; Approve/Edit/Reject applies only to analytical claims, never the artifacts; "Reject" logs to diary, doesn't delete. Dig Review is per-client only, never cross-client. Cross-client pattern intelligence (Knowledge System) is never shown to clients in attributable form.
Confirmed constraint: shared hosting (SureServer) can't run autonomous background scraping — V1 discovery stays manual. Standing principle: nothing already built is treated as sacred/fixed by default — expect orphaned files/schema throughout (confirmed repeatedly: SYSTEM_LEGEND.md found 7 duplication flags across client/case/entity/graph/memory data and 4 modules duplicating their matching engine; domain_id/ProjectManager trace never completed).
Caution: a local Ollama instance's system analysis was unreliable/hallucinated — not a trustworthy audit source.
Real Intake pillar spec = ingest form (ingest_module.php, canonical) + a Browse/Search companion (files_module.php + search_module.php).
Identity layer + Master OS/UI build (WO-I through WO-V) — all delivered, unverified against real prod until today
WO-I: tree_nodes + api/tree.php. WO-J: intake consolidation. WO-K: brain pipeline bound to session node_id. WO-L: Browse/Search pillar. WO-M: JSON->MySQL query switch. WO-N: engine file consolidation (later found to have overwritten real engine logic with stubs, fixed in WO-Q0). WO-O/P: Command View (cockpit_module.php, 4 widgets + 7 pillar tiles). WO-Q through WO-V: Case/Client drill-in shell + all 6 tabs (Timeline/Graph/Findings/Report/Payment/Email) built in client_view_module.php, routed via ?module=client_view&id=X.
Known unresolved issues from this build phase, to catch during/after live testing:
WO-R created a duplicate client-view system directly in shell.php (?module=clients&id=X) with a regressed placeholder Timeline — needs deleting; also graph_view.php may have overwritten a working WO-E version and originally assumed a wrong data source (fixed to read data/cases/{id}/network.json instead of a nonexistent cases.network_data column).
review_flags.context is assumed (not confirmed) to hold client_id; relationships table column names (relation_type/from_entity/to_entity) are unverified assumptions.
reports DB table is confirmed orphaned/unused (report_module.php never populates it) — vault_module.php's download now uses direct filesystem lookup instead.
payments table has no tier column — Exhibit Tier always shows N/A, and regenerated Stripe pay links lose tier info as a result.
Stripe checkout success/cancel URLs hardcoded to http:// — needs https before real use.
Cockpit's Clients&Cases pillar tile hardcoded to always link to client id=1 (demo stopgap) — blocks reaching the other 2 real clients until a real client list/Kanban exists.
A couple of unparameterized SQL queries recur despite being flagged (low risk, internal-only data, not urgent).
MAJOR resolved saga: SQLite -> MySQL + first live upload in weeks
System's DB layer was stuck on SQLite despite production being MySQL, through compounding bugs: config.local.php's override used define() after defaults were already set (PHP silently ignores re-definition); live config.php was an incomplete version missing the override logic entirely; a separate legacy root db.php was still hardcoded SQLite. SQLite was then removed entirely per John's decision — config.php loads config.local.php (real creds, created directly on the server via cPanel) for MySQL-only kernel_db().
CRITICAL CONTEXT: John uploaded to the live server for the first time in weeks during this session — everything before that (all of WO-A through WO-V) existed only on Windsurf's local mirror; live was running an old deployment. Today's uploads were piecemeal, not a full sync, so live is a patchwork of old files + today's fixes. This explains most of the schema-mismatch surprises below.
CORRECTION (8/19): the "3 real clients / 1 real case" seen below are NOT paying clients — they're test digs run against real local businesses (chosen for real history/data to exercise the system, since there's no finished OS yet to run a real engagement through). Most of that data is copy-pasted from LLM query outputs, not data the excavation pipeline itself actually gathered/ingested. No live paying clients exist yet — this reframes the "verify on a real case" plan as "verify on test-dig data, then separately confirm the excavation pipeline works on genuinely pipeline-sourced (not copy-pasted) data."
CONFIRMED via genuinely real (not simulated) db_verify.php runs: live has 3 real clients, 1 real case, and 39 total tables (many — users/roles/permissions/audit_logs/conversations/tasks/intake_queue/node_attributes — never referenced in any WO, unmapped territory). tree_nodes table pre-existed on live from an old deployment with a DIFFERENT real schema than WO-I assumed (no column literally named 'id') — CREATE TABLE IF NOT EXISTS had silently no-opped against it. Both memory_ingest (canonical) and memory_ingests (orphaned plural) exist as real tables.
STANDING LESSON: Windsurf/Gemini's chat CANNOT execute live DB queries at all — only reads/writes local files. Multiple attempts at "run this and tell me" produced fabricated "simulated" answers, including one flatly contradicting already-confirmed real data. Real DB verification must always go through an uploaded diagnostic PHP script visited directly by John in a browser (the db_verify.php pattern) — never trust Windsurf's chat claims about live DB state.
STANDING LESSON: Windsurf's "file created" claims have twice not matched reality (migration files 001/002 narrated as created but never existed on disk anywhere) — verify with a real directory listing when in doubt.
Live post-sync bug-fixing (current phase, in progress)
Working through real errors as pages get visited live for the first time. Recurring bug class: duplicate kernel_db() declarations across THREE separate files (kernel/db.php the real one; kernel/kernel_boot.php had its own hardcoded copy; root-level db.php also had its own) — same failure pattern as the original early-project kernel_validate_runtime() duplicate bug. Fixed by: kernel_boot.php now requires db.php instead of redefining; root db.php reduced to a passthrough (require_once kernel/db.php; global $pdo; $pdo = kernel_db(); — the global $pdo line needed because vault_module.php/report_module.php expect that legacy global, not just the function).
SECURITY ISSUE (important, recurring): kernel_boot.php's old hardcoded kernel_db() had the REAL live MySQL username/password embedded in plaintext, which got pasted into this chat TWICE (once when Windsurf showed its own fix, once when John pasted the still-unfixed live file to prove WO-Q12 hadn't actually deployed). John was told multiple times to rotate that MySQL password via cPanel — status as of last check still UNCONFIRMED, worth asking again before treating the DB layer as secure. Real credential value never stored in memory.
Other real bugs found and fixed live: cockpit was never added to kernel/module_registry.php despite being built (WO-Q11, one-line fix) — dig_review shows the same "(Not Yet Working)" broken-link symptom in the nav, likely the same missing-registry-entry bug, not yet confirmed/fixed. review_flags table didn't exist in live MySQL (dig_review's auto-create logic never fired since that page was never visited) — created manually via a one-time script; first attempt failed because MySQL (unlike SQLite) rejects a literal DEFAULT value on TEXT columns, fixed by using VARCHAR for status/source_table. Currently debugging: "headers already sent" warnings on report_module.php's PDF generation — dompdf actually IS generating valid PDFs, but report_module.php (and likely vault_module.php's download action too, untested) is being loaded as a normal embedded module fragment inside shell.php's already-started HTML page, so it can't set download/PDF headers. Fix in progress (WO-Q13): intercept report-generate and vault-download actions at the very top of shell.php, before any HTML output, and exit immediately rather than routing them through the normal module-fragment renderer.
Standing rule for this whole phase: whenever a file fix keeps failing to actually reach the live server via Windsurf, give John complete ready-to-paste file content directly and have him upload it himself, bypassing Windsurf's upload step entirely — this has proven necessary multiple times (kernel_boot.php needed this twice).
Password rotation CONFIRMED DONE — this item is closed.
WO-Q13 confirmed working: Report tab PDF now downloads clean with no warning text mixed in.
WO-Q14 fix fully confirmed correct after a follow-up correction: report_module.php and payment_module.php (including the sendPaymentNotifications() email-tracking updates, initially missed then fixed) all now use direct kernel_db() calls throughout, no lingering global db dependency anywhere. This whole thread (redeclare bugs + $db/ pdo inconsistency) is closed.
WO-Q15 issued: check whether dig_review is missing from kernel/module_registry.php (same symptom cockpit had — "(Not Yet Working)" broken link in nav) and fix the same way; also check "Predictive" and "Predictive Link V1" nav items which showed the identical symptom.
WO-Q15 partial: predictive/predictive_link_module_v1 registry mixup fixed correctly (predictive was wrongly pointing at predictive_link_module_v1.php's file, now split into two correct entries). dig_review was found to already be correctly registered — meaning its "(Not Yet Working)" symptom is NOT a registry issue like cockpit's was. Real likely cause identified: shell.php's sidebar status flag (the same field that was flipped 'broken'->'working' for cockpit in WO-O) is probably still set to not-working for dig_review, causing the sidebar to render the generic placeholder href instead of a real link — the module itself is likely fine since dig_review_module.php was core, already-tested V1 functionality (WO-D/F). WO-Q16 issued: flip dig_review's (and predictive/predictive_link_module_v1's) sidebar status flags, then visit shell.php?module=dig_review directly to confirm the module itself works regardless of the sidebar link bug.
WO-Q16 confirmed complete: dig_review and predictive status flags flipped 'broken'->'working' in shell.php's sidebar array (predictive_link_module_v1 was already correct). All three confirmed loading cleanly. This closes out the entire live-debugging punch list from today's post-sync session (DB consolidation, 3-way kernel_db() redeclare, review_flags creation, PDF header handling, blank report fields, nav status flags). System is now at a stable, functioning baseline. Remaining items are refinement, not broken-page fixes: WO-R's shell.php duplicate client-view cleanup, confirming graph_view.php wasn't a lossy overwrite, review_flags.context/relationships column assumptions, orphaned reports table decision, missing payments.tier column, Stripe http->https fix.
NEW real live error found (System Health page): processing_queue table doesn't exist in the live MySQL database. This is likely bigger than one page — processing_queue is referenced throughout the codebase (ai_process.php, task_generator.php, cockpit's "Needs Your Attention" widget, client drill-in's "Next Action" field per WO-Q). Confirmed from the earlier real db_verify.php table list: processing_queue was never in the live 39-table list — but intake_queue and tasks were, and might be the old deployment's equivalent under a different name. WO-Q18 issued (diagnosis only, no fix yet): DESCRIBE both intake_queue and tasks, compare their columns/data against what the code actually expects from processing_queue, report which is the real functional match vs. whether processing_queue genuinely needs creating fresh (like review_flags did) to avoid adding another orphaned/duplicate table.
WO-Q19 confirmed complete: all three int-to-htmlspecialchars() spots in cockpit_module.php fixed with (string) casts (case_id, ingest_number, authority_score), plus added ?? '' null-coalescing for extra robustness. Cockpit module should now render clean end to end.
MAJOR STATUS CORRECTION (8/23): despite prior "confirmed working" notes above, John's actual lived experience of the system is that it is not helpful — graph_view does not render a graph, only raw numbers (node/edge counts with no visual, no labels, no drill-down); ingest does not actually save data; the UI redesign made things "slightly better" but the system overall remains not usable day to day. This corrects/supersedes the earlier "graph_view.php confirmed working, tested 22 nodes/20 edges" note — that was apparently a technical/execution confirmation only, not a confirmation that a human could see or use the result. Standing lesson: "confirmed working" in this system's history has repeatedly meant "executes without erroring," not "produces legible human-facing output" — these must be tracked as separate status fields going forward, not conflated.
Ran a real macro-level system audit (8/23) of the "Graph & Entities" sidebar section (full codebase zip uploaded and read directly in Claude's workspace) per John's explicit standard: not "does the PHP execute" but does the module connect to real/other data and can John read the result in plain language, not raw numbers/JSON. Findings, saved as LEGAiSEE_System_Audit_Graph_Entities.md: shell.php hardcodes 'status' => 'working' for every module in this section regardless of actual behavior (not computed from any real check); the sidebar's "Network Graph" card routes to graph_module.php, which in module mode only returns two COUNT(*) numbers (Entities, Relationships) — the file's real path-finding logic is unreachable from the UI; a separate, working visual-graph renderer (graph_view.php, real vis-network) exists but is only wired into the Client View drill-in tabs, not the sidebar; root_cause_graph_module.php already writes the exact network.json file graph_view.php needs, but nothing wires the sidebar's Network Graph card to read it — a rewiring fix, not new development; Relations, Root Cause, Semantic Cluster, and Cross-Case Engine modules all generate correct underlying data but return it as raw pretty-printed JSON text blocks instead of human language — 4 of 7 modules in this section have a translation/presentation gap, not a data/logic gap; Clusters module (cluster_module.php) is the one module in the section that both connects to real data and renders genuinely readable HTML — flagged as the template pattern to replicate elsewhere. Audit approach established as the standing method going forward: unzip full codebase into Claude's own workspace (bash/view tools) rather than pasting individual files into chat, cross-check against legaisee-master-system-law as spec, continue section by section through the rest of shell.php's sidebar (Intelligence Engines, Excavation/Ingest, Tools) to build the complete system-level picture.
NEW direction for next session: current UI/CSS (dark yellow text, brown cards) reads as muddy/gloomy, not something John wants to stare at all day. Wants a real visual upgrade — brighter, more card-based and tab-based layout, closer to a real CRM feel, with HoneyBook as the explicit reference point (consistent with the earlier-locked Kanban design goal). This is a frontend/CSS pass, separate from today's backend/DB work — next thing to tackle in a future session.
Windsurf produced a full React/TypeScript rewrite proposal (HONEYBOOK_TO_LEGAISEE_ARCHITECT_INSTRUCTIONS.md, ~929 lines) in response to the UI-brightening request — proposes replacing the entire PHP/MySQL shell.php system with a new React SPA (Vite, TypeScript, Ant Design, React Query, Zustand), 12-week estimate, "API Service Layer" entirely unimplemented stubs. Flagged as a scope/tooling mismatch (abandons all of today's PHP debugging work, introduces a toolchain mismatched to John's non-coder Architect+Executor workflow and SureServer's PHP shared hosting).
IMPORTANT CORRECTED UNDERSTANDING: the "brighter UI" ask was too narrowly read as cosmetic. John's actual intent is much bigger — he wants to run Legaisee's day-to-day business operations THROUGH this system, with HoneyBook's full functional range (Home, Projects/Pipeline, Calendar, Contacts, Galleries, Lead Forms, Files, Finance, Services, Templates) as the operational backbone, and LEGAiSEE's excavation/dig-review/knowledge-system capabilities integrated INTO that CRM foundation — not a CRM skin bolted onto an excavation-first system. His explicit framing: "I want it to make money every day," not just look nicer. He downloaded HoneyBook's pages and asked Windsurf directly whether it could be cloned/adapted, which produced the React rewrite proposal — he did not ask for a rewrite himself.
REFINED VISION: not two systems side by side but genuinely interleaved/merged page by page, "like shuffling a deck of cards where it makes sense." His own examples: (1) login page = HoneyBook's login shell landing on LEGAiSEE's "Good Morning John" Cockpit overview; (2) HoneyBook's client management page merges with LEGAiSEE's client drill-in portal into one page; (3) LEGAiSEE's own self-maintenance tasks get scheduled (e.g. Tuesday midnight) and appear as events in the Calendar view alongside client work. Overall goal explicitly stated: a stable, actually-usable production V1 he can run comfortably for a full 13 months without having to chase bugs while simultaneously managing real clients — stability/usability is the top priority, not maximum feature completeness or a rewrite.
MAJOR CORRECTION to the merge map, from a detailed walkthrough of real HoneyBook page content with John's own annotations: "Contacts" and "Client View/Drill-In" are two separate things, not one. Contacts = a separate lightweight A-Z rolodex of everyone (leads, clients, vendors, referral sources) with a simple contact card (info, labels, conversation/action log), filterable by type/status. Client View/Drill-In = reached ONLY by clicking a client card in the Pipeline Kanban (not from Contacts) — this is the deep, in-system-clients-only ("money makers") view, one seamless page split into two halves that must feel like a single view despite pulling from multiple system modules: Half A = HoneyBook-style operational info (activity feed, files, tasks, financials, notes, contact details), Half B = LEGAiSEE-native — Excavation/Dig Review/Knowledge System live here as a SUB-SELECTION within Client View, not as their own top-level nav items, plus a per-client Vault view (every artifact found for that client — wayback snapshots, photos, videos, PDFs, system memory, portal data — with graph nodes/relations/mention counts). Today's six built drill-in tabs (Timeline/Graph/Findings/Report/Payment/Email) become Half B, to be merged with a new Half A rather than discarded.
"Galleries" was wrongly flagged as droppable in the first draft — corrected: Galleries becomes the system-wide LEGAiSEE Vault (cross-client artifact repository tracking relations/actions/diagnoses/persistent memory/suggestions/system setups/monitoring across all clients, filterable down to one client).
New areas surfaced from the full HoneyBook walkthrough not in the first draft: Scheduler (session types/booking links/availability, feeds Calendar), standalone Task Management (separate from client-specific tasks, mirrors onto Home's Tasks widget and Calendar's daily to-do), Automations (trigger/action/condition builder, AI-assisted — V2, matches existing OS-vision self-maintenance/daily-report wishlist), Reports/Insights (booking rate, lead source breakdown, monthly revenue — maps to Knowledge System/business-intelligence goals), Integrations/Settings (Gmail, Google Contacts, QuickBooks, Zapier, brand settings — lower priority but should exist eventually). Calendar also needs: month/week/day views, today's to-do list mirrored on Home, and Google Calendar import/sync.
Business/product decision (separate from the build itself): John wants to eventually remove pricing from the public website entirely and shift to a "contact for pricing" / invitation-only vault feel, with a separate hidden page or PDF sent to qualified leads instead.
CONFIRMED top-level nav structure (John signed off): Home, Pipeline (Clients/Cases Kanban), Contacts (rolodex), Calendar, Vault (system-wide, was "Galleries"), Files, Finance, Services, Templates, Lead Forms, Tools (Scheduler, later Automations), Reports (later/V2), Settings (later/V2) — with Client View/Drill-In living outside that nav, reachable only via Pipeline or Contacts. Ready to be turned into real spec-level work orders, area by area, whenever John resumes.
Clarification: the standalone Task Management item in the nav is intentionally small — a lightweight to-do list linked to Calendar, not the real project-management system. The full-scope Legaisee Project Manager (separate, confirmed not working, own MVC — see earlier ProjectManager notes) is still its own outstanding item and must not be conflated with this small Calendar-linked task widget.
New required feature: client-facing drag-and-drop artifact upload system in the Client Portal/Vault — clients upload their own source material (PDFs, photos, video, scans of documents, reports, pasted text/data, database exports, website content) directly into their own artifact archive. Framed as putting "the company college assistant to work on a real task instead of filing or shredding papers" — client-supplied artifacts should feed the same ingest/Vault system as AI-found and John-found artifacts, tagged with distinct provenance (client-provided vs. self-found vs. AI-found) so Dig Review always knows the source.
Full macro system audit completed (8/23) across all 7 shell.php sidebar sections (Command, Planning, Clients & Cases, Excavation, Intelligence Engines, Graph & Entities, Tools) — ~30 modules, saved as 5 files (John is logging these to Google Docs). Whole-system pattern identified: most underlying logic across the app is genuinely correct/sophisticated; the near-universal failure is presentation — traced to two shared rendering functions in shell.php (render_inspector dumps raw JSON into a <pre> box; the exec-panel fallback silently collapses any array value down to just its count, confirmed to destroy Predictive Link V1's real output). A third, well-built human-legible rendering system (modules/utils/uic_v1.php — uic_artifact/uic_dataset/uic_system) already exists in the codebase and is used by only one module (Intelligence Dashboard) — identified as the cheap fix path, since most JSON-dumping modules already have correct data and just need routing through this instead of json_encode(). Confirmed working-as-intended (reference examples): Cockpit, Clusters, Ingest View, Files, Search, Dig Review, Client Vault, Cases, Project Board, Intel Dashboard, Recommendation. Standalone findings beyond presentation: (1) Insight module (modules/insight_module.php) is a mis-wired duplicate of the Ingest module's code, writing to different/conflicting memory_ingest column names than real Ingest uses — plausible partial cause of "ingest doesn't save"; (2) Memory Ingest's file-upload path has a confirmed silent-failure bug (leftover SQLite datetime('now') syntax + an empty catch block) that can show "uploaded successfully" even when the DB insert failed; (3) three cases of real, working logic orphaned/disconnected from the module the sidebar actually calls — Root Cause writes network.json that Network Graph never reads, ui/compare_engine.php (real comparison renderer) is never referenced anywhere and Compare module is a functionless stub, and Vault (not Payments) holds the real Stripe checkout logic. Every module across the entire sidebar is still hardcoded 'status' => 'working' in shell.php regardless of any of this. Standing instruction: audit phase is complete — no fixes/work orders yet, John reviewing full audit in Google Docs first before scoping fix WOs.
Backend/orphaned-systems audit completed (8/23), saved as LEGAiSEE_System_Audit_Backend_and_Orphaned_Systems.md (John already copied this output before the session cut off). Headline finding: the 1,243-file codebase is not one system — it's the real live system (shell.php + modules/ + a working slice of engine/kernel) plus at least two dead/parallel systems layered on top. In engine/ (36 files), 15 have zero references anywhere in the codebase (goal_engine, goal_generator, goal_alignment, goal_arbitration_engine, goal_conflict_resolver, decision_governor, long_horizon_planner, temporal_memory_engine, pattern_stability, insight_mutator, governance_policy_engine, graph_mutation_engine, execution_feedback_engine, executive_dashboard_engine [note: different file from the live modules/executive_dashboard_module.php], cross_case_learning_compiler, plain_english_helper, system_behavior_spec) — together they read as a coherent but entirely unwired autonomous-agent architecture (goal-setting, arbitration, governance, long-horizon planning, self-evolving memory) that was designed but never connected to anything that runs. api.php (top-level router) has zero callers, meaning the entire api/v8/ directory (29 files: insights, compressor, predictor, dossier_v2, memory_store, agent, run_worker, etc.) is unreachable dead code. cli/ (9 files incl. meta_goal_engine, autonomous_run, chain_evolver, a second goal_engine.php) is only reachable through index.php — a separate, self-labeled "<!--TEST-->" prototype entry point that reads/writes its own flat JSON files (bypassing the real MySQL-backed system entirely) and contains a GET-triggered ?run_worker=1 that shell_execs cli/worker.php with no visible auth check. .htaccess has no rule redirecting the bare site root to shell.php in the audited zip — CORRECTED (8/23, later same day): John confirmed the bare root URL (https://www.legaisee.com/commandcenter/) already lands on Grand Lobby (shell.php) via an .htaccess fix made "yesterday" — the zip's .htaccess was outdated relative to the live server. Remaining real exposure, narrower than originally flagged: index.php is still directly reachable if hit by its exact URL/filename (confirmed live by John pasting its output) — the redirect only governs the bare folder URL, not the filename itself — so the ?run_worker=1 trigger (shell_execs cli/worker.php, no auth check visible in code) is still reachable by anyone who knows or bookmarks that exact URL. Live test also showed: Executive Dashboard and Memory Evolution panels return "File not found" (expected data files never generated), Predictive/System Insights show real computed JSON, and the Worker Output panel threw a PHP deprecation warning on a plain page load (not just on clicking Run Worker) — confirming some of index.php's logic executes automatically just by visiting the page. index.php's client folder data (test, test2, test_client_*, client_001-005 flat JSON) is confirmed separate from/inconsistent with the real MySQL-backed client data shell.php's Client Vault shows. ExcavationCommand/ (3 files) and ProjectManager/ (88 files) both confirmed to have zero live references from shell.php/modules — structurally disconnected, not just buggy (ProjectManager already known as "not working yet," now confirmed fully orphaned too). Only genuinely live backend pieces outside modules/: api/search_load.php and api/files_action.php (confirmed real, called via JS fetch from Search/Files modules), plus roughly a dozen real engine/ files (entity_extraction_engine, cluster_engine, cross_case_engine, recommendation_engine, system_insight_engine, decision_engine_v2, policy_enforcement_engine, portfolio_engine, semantic_cluster_engine, system_recommendation_engine, dashboard_engine, reasoning_engine) and kernel/. Practical takeaway: a large share of the perceived system size/complexity is not live surface area — it's dead code, a superseded prototype, and disconnected sub-apps. Four open questions flagged for a live check (not code-answerable): does cron actually run cli/worker.php; does the bare site URL serve index.php's old prototype instead of shell.php; is index.php's ?run_worker=1 trigger reachable with no auth; and whether ProjectManager/ExcavationCommand should be revived or formally retired. This completes the full audit (7 sidebar sections + backend/orphaned-systems layer) — still no fix work orders yet per standing instruction, audit output being reviewed in Google Docs first.
Deep-dive on the 15 orphaned engine/ files completed (8/23), saved as LEGAiSEE_Orphaned_Engine_Files_Explained.md, answering John's question of what they do/why built/why disconnected/should they be wired in. Major finding: engine/plain_english_helper.php is a ready-built, unused fix for the audit's #1 finding — its own header states "Every module currently shows raw numbers. This helper converts them into sentences a client can read," and it already has working translator functions (lee_interpret_relations, _graph, _anomaly, _insight, _recommendation, _cluster, _cross_case, _time_intelligence, plus a generic fallback) matching most of the modules flagged as JSON-dumping in the earlier audits. Nothing calls it — recommended as near-top fix priority since it requires wiring, not new development. The remaining 14 files (goal_generator, goal_alignment, goal_arbitration_engine, goal_conflict_resolver, pattern_stability, long_horizon_planner, decision_governor, governance_policy_engine, graph_mutation_engine, execution_feedback_engine, cross_case_learning_compiler, temporal_memory_engine, memory_evolution_engine, system_behavior_spec) form one coherent, real (not junk) unfinished pipeline for an autonomous goal-generation/planning/governance/learning system — goals proposed from graph signals, filtered/ranked/deconflicted, checked for statistical stability, planned over a long horizon, gated through a decision governor against hard constraints (max 10 active goals, max 200 queued tasks, max 15 mutations/cycle), executed, measured via feedback, learned from across all cases, and remembered over time. This maps directly onto the legaisee-authority-engine vision doc (recommendation lifecycle, versioned system beliefs, quarterly evolution) — assessed as likely a prior AI "architect mode" session that started building toward that vision and ran out of session budget before final wiring, not abandoned because it was wrong. Recommendation given: keep the files (don't delete), but don't wire this cluster in yet — it depends on foundations (real graph wiring, trustworthy presentation) this audit found are not yet solid, and activating it now risks becoming the exact kind of open-ended scope creep John has repeatedly said he wants to avoid. Nothing currently depends on it, so no feature is lost by leaving it inactive for now.

